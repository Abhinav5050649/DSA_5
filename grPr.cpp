#include "bits/stdc++.h"
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cout.tie(0);
    cin.tie(0);

    
    ll n, m; cin >> n >> m;
    
    vector<vector<int>> adj(1e5 + 1, 0);

    while (temp--)
    {
        ll num; cin >> num;

    }
    return 0;
}